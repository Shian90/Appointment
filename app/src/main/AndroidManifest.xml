<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_ointmentt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomePagePatient"></activity>
        <activity android:name=".HomePageDoctor" />
        <activity android:name=".LoginDoctor" android:theme="@style/SplashTheme"/>
        <activity android:name=".LoginPatient" android:theme="@style/SplashTheme"/>
        <activity android:name=".RegisterDoctor" android:theme="@style/SplashTheme"/>
        <activity android:name=".RegisterPatient" android:theme="@style/SplashTheme"/>
        <activity android:name=".Module" android:theme="@style/SplashTheme"/>
        <activity android:name=".MainActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>